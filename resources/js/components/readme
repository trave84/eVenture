npm init

1) INDEX.HTML = views/app.blade.php
2) NO webpack.config.js where:
  'plugins:[new HTMLPlugIn({
    template: './resources/index.html'})]'

3) NO package.json:
    "devDependencies" {
        "babel-preset-react": ""
    }

3) NO PRESET KNOWN TO USE: 
    .babelrc where {
      "presets": ['env', 'react'] 
    }

4)  IN index.jsx:
      import React from 'react';
      import ReactDOM from 'react-dom';
      import App from /.components/App;

      ReactDOM.render(<App />, document.getElementById('app')); 

      [in Views/app.blade.php NOW]


